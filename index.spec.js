import { describe, test } from 'vitest'

describe('addTea', () => {
    test.todo('should create a new tea')
    test.todo('should succeed if saveTea succeeds')
    test.todo ('should fail if saveTea gives an error')
});