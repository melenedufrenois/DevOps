import { describe, test } from 'vitest'

describe('getTeaByName', () => {
    test.todo('should return the Tea associated to the name given')
    test.todo('should return undefined if the Tea does not exist')
})

describe('saveTea', () => {
    test.todo('should fail if Tea name already exists')
    test.todo('should fail if Tea id already exists')
    test.todo('should save the new Tea if it does not already exist')
})

describe('generateNewTeaId', () => {
    test.todo('should generate a unique id')
})